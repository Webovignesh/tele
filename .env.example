# Optional: if not set, you can enter credentials via the web UI
# at http://localhost:3000 (they will be saved to config.json).
API_ID=1234567
API_HASH=0123456789abcdef0123456789abcdef

# HTTP port for the web app
PORT=3000

# How many files download at the same time (aggregate speed scales with this)
CONCURRENCY=5
